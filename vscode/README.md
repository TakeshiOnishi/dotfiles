# VS Code設定ガイド

このディレクトリには、Neovim設定から変換したVS Code設定ファイルが含まれています。

```bash
# VS Code設定ディレクトリへシンボリックリンクを作成
ln -sf ~/dotfiles/vscode/keybindings.json ~/Library/Application\ Support/Code/User/keybindings.json
```